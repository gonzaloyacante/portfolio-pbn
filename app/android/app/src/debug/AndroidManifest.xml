<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- ─────────────────────────────────────────────────────────────────────
         Label distinto para el build DEBUG.
         Esto permite distinguir visualmente la app debug de la release:
         - Release (packageId: es.paolabolivar.admin)      → "PBN Admin"
         - Debug   (packageId: es.paolabolivar.admin.debug) → "PBN Admin [DEV]"
         Sin esto ambas apps muestran el mismo nombre "PBN Admin" en el launcher
         y el usuario no puede saber cuál es cuál.
         ───────────────────────────────────────────────────────────────────── -->
    <application android:label="PBN Admin [DEV]" />
</manifest>
