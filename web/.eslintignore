# eslintignore - Compatibility file for Codacy (uses ESLint 8 internally)
# These files use Node.js globals (process, __dirname) not recognized by ESLint 8 flat config

# Scripts use Node.js globals directly
scripts/

# Prisma seed files use Node.js globals
prisma/

# Test files use Vitest globals (describe, it, expect, afterEach, etc.)
tests/
