// ============================================
// EMAIL SETTINGS SEED - Notification configuration
// ============================================

export const emailSettings = {
  id: 'default-email',
  sendContactNotifications: true,
  sendBookingNotifications: true,
  sendTestimonialNotifications: true,
  notificationEmails: [] as string[],
}
